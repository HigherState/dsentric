package dsentric.contracts

import org.scalatest.EitherValues
import org.scalatest.funspec.AnyFunSpec
import org.scalatest.matchers.should.Matchers

class UnapplySpec extends AnyFunSpec with Matchers with EitherValues {

  //TODO check nested pathing failure, ie if path fails, shouldnt return default values
  //  or Empty object if (Expected object with no values)
}
